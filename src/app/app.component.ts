import { Component } from '@angular/core';
import { MainSearch } from './Components/Main.component';

@Component({
  selector: 'my-app',
  template: `<Search-wrapper></Search-wrapper>`,
})

export class AppComponent  { name = 'Angular'; }
